#include <stdio.h>
#include <vector>
#include <iostream>

using namespace std;

int main() {
	int blocks,m,g,x;
	vector < pair<int,int> > lista;
	scanf("%d",&blocks);
	while(blocks>0){
		scanf("%d",&m);
		printf("%d ",m);
		while(m>0){
			lista.push_back(tmp);
			m--;
		}
		scanf("%d",&g);
		while(g>0){
		
			g--;
		}
		blocks--;
	}
	return 1;
}

